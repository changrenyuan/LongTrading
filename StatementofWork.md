# MT_Alpha 自动化交易系统：项目开发任务书 (Statement of Work)

**版本：** v1.0

**日期：** 2026年2月27日

**状态：** 规划中（待确认）

---

## 一、 项目背景与目标

本项目旨在为资深交易员开发一套高度稳健、安全、可视化的 Python 自动化交易系统。系统早期侧重于低频策略的实盘验证与养老级稳定性，架构上预留高频交易扩展接口。

## 二、 系统逻辑架构

系统采用模块化、事件驱动的设计模式，确保各个组件相互独立。

### 1. 核心模块定义

- **Data Gateway (行情网关)**：对接交易所 API，处理 REST/WebSocket 数据流。

- **Strategy Alpha (策略引擎)**：封装私密交易逻辑，计算入场与离场信号。

- **Risk Guard (独立风控)**：前置校验模块，负责止损、仓位控制及紧急停机。

- **OMS (订单管理)**：负责订单精度处理、状态追踪及执行优化。

- **Web Dashboard (前端看板)**：基于 Streamlit/FastAPI 的实时收益与状态显示。

## 三、 工作量汇总与实施计划

| **阶段**         | **交付物** | **核心功能**                 | **周期** |
| -------------- | ------- | ------------------------ | ------ |
| **P1: 底层搭建**   | 基础运行环境  | API 认证、加密配置、异步心跳检测、结构化日志 | 1 周    |
| **P2: 策略建模**   | 逻辑执行模块  | 策略信号代码化、多周期数据对齐、状态机管理    | 1.5 周  |
| **P3: 回测审计**   | 策略评估报告  | 历史数据回测、夏普比率、最大回撤可视化分析    | 1 周    |
| **P4: 风险防护**   | 哨兵报警系统  | 钉钉/微信实时预警、硬件级止损保护、异常自愈   | 0.5 周  |
| **P5: Web 看板** | 远程监控后台  | 资产曲线、实时日志流、参数远程动态修改      | 1 周    |

## 四、 顶级工业标准对比 (Gap Analysis)

本系统在设计上参考了顶级量化工具的标准，具备以下特质：

- **幂等性执行**：确保在网络波动时不会产生重复下单。

- **状态持久化**：系统重启后可自动检索本地数据库，接管现有持仓。

- **低延迟设计**：核心逻辑基于 `Asyncio` 异步 IO 构建，为未来秒级交易做准备。

---

## 五、 交付清单 (Deliverables)

1. **System Source Code**：完整的 Python 源代码及环境依赖包。

2. **Backtest Report**：策略历史表现对比审计报告（PDF/HTML）。

3. **Deployment Guide**：服务器部署及 API 密钥配置手册。

4. **Admin Portal**：可多端访问的 Web 交互式后台。
